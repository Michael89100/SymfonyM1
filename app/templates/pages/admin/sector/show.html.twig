{% extends 'base.html.twig' %}

{% block title %}Sector{% endblock %}

{% block body %}
    <h1>Sector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sector.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sector.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sector.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sector.index') }}">back to list</a>

    <a href="{{ path('sector.edit', {'id': sector.id}) }}">edit</a>

    {{ include('pages/admin/sector/_delete_form.html.twig') }}
{% endblock %}
