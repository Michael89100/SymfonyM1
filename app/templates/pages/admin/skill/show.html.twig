{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skill.index') }}">back to list</a>

    <a href="{{ path('skill.edit', {'id': skill.id}) }}">edit</a>

    {{ include('pages/admin/skill/_delete_form.html.twig') }}
{% endblock %}
